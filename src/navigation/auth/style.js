import { StyleSheet } from "react-native";

function createStyle() {
  const styles = StyleSheet.create({
    tabBarStyle: { height: 85, paddingTop: 0, paddingBottom: 30 },
  });
  return styles;
}
export default createStyle;
